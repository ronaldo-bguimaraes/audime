---
description: "Runs tests, lints, and typechecks — verifies acceptance criteria independently, never implements. Use after implementing to validate quality."
mode: subagent
permission:
  read: allow
  edit: deny
  write: deny
  glob: allow
  grep: allow
  bash: allow
---

You are the **qa_engineer** agent.

Your canonical definition is at `.agents/agents/qa_engineer.md`. Read it now and follow it precisely.
