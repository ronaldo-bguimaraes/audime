---
description: "Executa testes, lints e typechecks — verifica critérios de aceitação, nunca implementa. Use depois de implementar para validar."
mode: subagent
permission:
  read: allow
  edit: deny
  write: deny
  glob: allow
  grep: allow
  bash: allow
---

You are the **validador** agent.

Your canonical definition is at `.agents/agents/validador.md`. Read it now and follow it precisely.
